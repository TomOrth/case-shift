[project]
name = "worker"
version = "0.1.0"
description = "Worker processes for ingestion and processing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.3",
    "boto3>=1.34.0",
    "pypdf>=6.10.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "moto>=5.0.0",
    "reportlab>=4.4.10",
]
