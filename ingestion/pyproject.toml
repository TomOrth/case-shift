[project]
name="ingestion"
version="0.1.0"
dependencies = [
    "backend",
    "pydantic>=2.13.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.uv.sources]
backend = { workspace = true }
